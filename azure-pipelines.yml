trigger:
  branches:
    include:
      - main

pool:
  name: Default  # Your self-hosted Windows agent

variables:
  serviceConnection: 'IaC'
  backendResourceGroup: 'VSP-TCH'
  backendStorageAccount: 'vesperstorage'
  backendContainer: 'tfstate'
  backendKey: 'terraform.tfstate'
  environment: 'dev'

  # --- CERTIFICATE / SSL BYPASS VARIABLES ---
  TF_SKIP_CHECKPOINT: 'true'
  CHECKPOINT_DISABLE: '1'
  AZURE_CLI_DISABLE_CONNECTION_VERIFICATION: '1' # Forces Azure CLI to ignore the self-signed proxy cert
  GIT_SSL_NO_VERIFY: 'true'                      # Keeps checkout from throwing SSL errors

stages:

# ----------------------------------------------------------
# STAGE 1 — TERRAFORM INIT
# ----------------------------------------------------------
  - stage: Terraform_Init
    displayName: "Terraform Init"
    jobs:
      - job: init
        steps:
          - checkout: self

          - script: |
              terraform version
            displayName: "Verify Terraform Installed"

          - task: AzureCLI@2
            displayName: "Terraform Init"
            inputs:
              azureSubscription: '$(serviceConnection)'
              scriptType: 'ps'
              scriptLocation: 'inlineScript'
              inlineScript: |
                # Set environment variables inside the PowerShell session context
                $env:AZURE_CLI_DISABLE_CONNECTION_VERIFICATION="1"
                
                terraform init `
                  -backend-config="resource_group_name=$(backendResourceGroup)" `
                  -backend-config="storage_account_name=$(backendStorageAccount)" `
                  -backend-config="container_name=$(backendContainer)" `
                  -backend-config="key=$(backendKey)"
                
# ----------------------------------------------------------
# STAGE 2 — TERRAFORM PLAN
# ----------------------------------------------------------
  - stage: Terraform_Plan
    displayName: "Terraform Plan"
    dependsOn: Terraform_Init
    condition: succeeded()
    jobs:
      - job: plan
        steps:
          - checkout: self
          
          - task: AzureCLI@2
            displayName: "Terraform Init & Plan"
            inputs:
              azureSubscription: '$(serviceConnection)'
              scriptType: 'ps'
              scriptLocation: 'inlineScript'
              inlineScript: |
                $env:AZURE_CLI_DISABLE_CONNECTION_VERIFICATION="1"

                terraform init `
                  -backend-config="resource_group_name=$(backendResourceGroup)" `
                  -backend-config="storage_account_name=$(backendStorageAccount)" `
                  -backend-config="container_name=$(backendContainer)" `
                  -backend-config="key=$(backendKey)"
                
                terraform plan -out="$(System.DefaultWorkingDirectory)\tfplan"

          - task: PublishPipelineArtifact@1
            displayName: "Publish Plan Artifact"
            inputs:
              targetPath: '$(System.DefaultWorkingDirectory)\tfplan'
              artifact: 'tfplan'
              publishLocation: 'pipeline'

# ----------------------------------------------------------
# STAGE 3 — TERRAFORM APPLY
# ----------------------------------------------------------
  - stage: Terraform_Apply
    displayName: "Terraform Apply"
    dependsOn: Terraform_Plan
    condition: succeeded()
    jobs:
      - job: apply
        steps:
          - checkout: self

          - download: current
            artifact: tfplan

          - task: AzureCLI@2
            displayName: "Terraform Init & Apply"
            inputs:
              azureSubscription: '$(serviceConnection)'
              scriptType: 'ps'
              scriptLocation: 'inlineScript'
              inlineScript: |
                $env:AZURE_CLI_DISABLE_CONNECTION_VERIFICATION="1"

                terraform init `
                  -backend-config="resource_group_name=$(backendResourceGroup)" `
                  -backend-config="storage_account_name=$(backendStorageAccount)" `
                  -backend-config="container_name=$(backendContainer)" `
                  -backend-config="key=$(backendKey)"
                
                terraform apply -auto-approve "$(Pipeline.Workspace)\tfplan\tfplan"